name: checks

on:
  pull_request:
  push:
    branches: [main]

jobs:
  checks:
    uses: fleetorders/.github/.github/workflows/fleet-checks.yml@v1
    with:
      test: npm test
      lint: npm run lint --if-present
