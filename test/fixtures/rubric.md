# Rubric: release notes

The alarm is a release-notes excerpt. Assess whether it mentions a change to
the CLI flags, settings keys, hooks or transcript fields the integration
declares in its manifest.
