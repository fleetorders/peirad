# Harness changelog — 2.2.0

## Breaking changes

- The `--allowedTools` flag is now `--allowed-tools`; the old spelling is no longer accepted.
- Hooks now receive a `tool_input` object instead of separate fields.

## Other

- Performance improvements and bug fixes.
