---
"peirad": minor
---

The verdict names the checker that produced it: the header carries `peirad <version>` beside the harness version, and `--json` carries it as `checker`. A wiring pinned to one release knows which build spoke; one that tracks the newest release did not, and a line nobody can attribute to a build is not evidence.
